<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= product.name %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" />
    <script src="	https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container">
        <h1>Edit Product</h1>
        <form method="post" action="/products/<%= product._id %>?_method=PUT">
            <label for="name" class="form-label">Name</label>
            <input type="text" name="name" id="" placeholder="Product Name" class="form-control mb-3"
                value="<%= product.name %>" />
            <label for="price" class="form-label">Price</label>
            <input class="form-control mb-3" type="number" name="price" id="" min=0 step=0.1 max=100
                placeholder="Product Price" value="<%= product.price %>" />
            <label for="category" class="form-label">Category</label>
            <select name="category" class="form-select" aria-label="Default select example">
                <% for(let category of categories) {%>
                    <option value = "<%= category%>" <%= product.category === category ? 'selected' : '' %>><%=category %></option>
                  <%}%>
            </select>
            <div class = "d-grid">

                <button type="submit" class="btn btn-primary mt-3">Update</button>
            </div>
        </form>
            <a href="/products" style = "display: block; text-decoration: none;" class = "mt-3">
                <div class = "d-grid">
                    <button class = "btn-warning btn">Cancel</button>
                </div>
            </a>
    </div>
</body>

</html>